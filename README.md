# recognition_platform_client
This project is an example of how a client can use the recognition platform's outputs.

## What this does
This sample connects to the perception platform over WebSocket and uses live ASR and emotion outputs as interaction inputs.

## Setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt